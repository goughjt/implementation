#include <iostream>
#include "RedSVD.h"

//sing namespace RedSVD;
using namespace std;

int main(int argc, char* argv[])
{


/*int n = 2;
Matrix a_matrix = Matrix (n, n);

for (octave_idx_type i = 0; i < n; i++)
for (octave_idx_type j = 0; j < n; j++)
a_matrix(i,j) = (i + 1) * 10 + (j + 1);

FloatSVD f = FloatSVD(a_matrix);
Matrix b_matrix = f.right_sm;

std::cout << a_matrix;
std::cout << f.left_sm;
std::cout << f.sigma;
std::cout << f.right_sm;*/
  

  
  return 0;
}
